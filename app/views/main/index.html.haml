%h1 This is supposed to be a welcome page

- if current_user
  %p
    = 'You are logged in as ' + current_user.first_name + ' ' + current_user.last_name
  %p
    = link_to 'Logout', logout_path
- else
  %p
    = link_to 'Login', login_path
    = link_to 'Register', register_path
